'use strict'

// use this to control for the different game outcomes
const TOKEN = {
  X: 'X',
  O: 'O'
}
const OUTCOME = {
  X: TOKEN.X,
  O: TOKEN.O,
  DRAW: 'DRAW'
}

module.exports = {
  OUTCOME,
  TOKEN
}
